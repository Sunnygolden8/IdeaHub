<h1>Projects</h1>

<table class="table">
  <tr>
    <th>Title</th>
	<th></th>
    <th>Status</th>
    <th></th>
    <th></th>
  </tr>

<% @projects.each do |project| %>
  <tr>
    <td><%= project.title %></td>
    <td><%= project.user %></td>
    <td><%= project.status %></td>

  </tr>
<% end %>
</table>

<br />

<%= link_to( 'Sign in', new_user_session_path ) %>