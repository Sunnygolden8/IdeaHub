<div class="container">
	<p></p>
	<p><%= link_to( 'Home',root_path, class:"btn btn-default btn-xs")%></p>
	<p> <% if user_signed_in?%>
		<table class="table">
			<tr><td>
			<%= 'Hello, ' %>	<%= link_to( current_user.first_name + ' ' + current_user.last_name, user_path(current_user.id), class:"btn btn-default btn-xs")%>  </td><td><%= link_to('Sign Out', destroy_user_session_path, method: :delete, class:"btn btn-default btn-xs")%></td></tr></table>
		<% else%>
		<table class="table"><tr><td>
			<%= link_to('Sign In', new_user_session_path, class:"btn btn-default btn-xs")%>
		</td></tr></table>
		<%end%>
	</p>
	<p>
		<h1><%= current_user.first_name + ' '+current_user.last_name%></h1>
		<h3>Reputation: <%= current_user.reputation%></h3>
		<table class="table">
			<tr><th>Current Projetcs</th><th>Completed Projects</th></tr>
			<tr><td>List of current projects</td><td>List of past projects</td></tr>
		</table>
	</div>