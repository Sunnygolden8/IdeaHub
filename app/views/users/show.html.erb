<div class="container">
	<p></p>
	<p> <% if user_signed_in?%>
		<table class="table">
			<tr><td>
			<%= 'Hello, ' + current_user.first_name + ' ' + current_user.last_name %> </td><td><%= link_to('Sign Out', destroy_user_session_path, method: :delete, class:"btn btn-default btn-xs")%></td></tr></table>
		<% else%>
		<table class="table"><tr><td>
			<%= link_to('Sign In', new_user_session_path, class:"btn btn-default btn-xs")%>
		</td></tr></table>
		<%end%>
	</p>
	<p>
		<h1><%= current_user.first_name + ' '+current_user.last_name%></h1>
		<table class="table">
			<tr><th>Your Projects</th><th>Recent Projects</th></tr>
			<tr><td>Current Projects</td><td>Last 5 Projects</td></tr>
		</table>
	</div>